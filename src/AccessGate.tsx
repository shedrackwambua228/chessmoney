import { useEffect, useState, type FormEvent } from 'react'
import { ArrowUpRight, BookOpen, CircleDot, Crown, Puzzle, Swords } from 'lucide-react'
import App from './App'
import { api, ApiError } from './api'

type User = { id: string; name: string }
const position = ['♜','','♝','♛','♚','♝','♞','♜','♟','♟','♟','','','♟','♟','♟','','','','','♟','','','','','','','♟','','','','','','♗','','','♙','','','','','♘','','','♘','','','♙','♙','♙','','','♙','♙','♙','♙','♖','','♗','♕','♔','','','♖']

export default function AccessGate() {
  const [user, setUser] = useState<User | null | undefined>(undefined), [register, setRegister] = useState(false), [verificationEmail, setVerificationEmail] = useState(''), [message, setMessage] = useState(''), [error, setError] = useState(''), [busy, setBusy] = useState(false), [registeredUsers, setRegisteredUsers] = useState<number | null>(null)
  useEffect(() => { api<{ user: User }>('/auth/me').then(result => setUser(result.user)).catch(() => setUser(null)) }, [])
  useEffect(() => { api<{ registeredUsers: number }>('/auth/stats').then(result => setRegisteredUsers(result.registeredUsers)).catch(() => setRegisteredUsers(null)) }, [])
  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); setBusy(true); setError(''); setMessage(''); const form = new FormData(event.currentTarget)
    try {
      if (verificationEmail) setUser((await api<{ user: User }>('/auth/verify-email', { method: 'POST', body: JSON.stringify({ email: verificationEmail, code: form.get('code') }) })).user)
      else if (register) { const email = String(form.get('email') ?? ''); await api('/auth/register', { method: 'POST', body: JSON.stringify({ name: form.get('name'), email, password: form.get('password') }) }); setVerificationEmail(email); setMessage('We sent a six-digit verification code to your email.') }
      else setUser((await api<{ user: User }>('/auth/login', { method: 'POST', body: JSON.stringify({ email: form.get('email'), password: form.get('password') }) })).user)
    } catch (caught) { setError(caught instanceof ApiError ? caught.message : 'Please try again.') } finally { setBusy(false) }
  }
  if (user) return <App />
  if (user === undefined) return <main className="access-gate"><p>Opening the club…</p></main>
  return <main className="access-gate club-gate"><header className="club-header"><a href="#top" className="club-brand"><span><Crown size={18} /></span>mochess<span className="brand-dot">.</span></a><div className="club-header-note"><CircleDot size={13} /> THE CLUB IS OPEN</div><p>Free chess for curious minds.</p></header><div className="club-layout" id="top"><section className="club-position"><div className="position-label"><span>POSITION OF THE MOMENT</span><b>White to move</b></div><div className="club-board" aria-label="A chess position"><div className="board-coordinates files">a b c d e f g h</div>{position.map((piece, index) => <span key={index} className={(Math.floor(index / 8) + index) % 2 ? 'club-dark' : 'club-light'}>{piece}{index % 8 === 0 && <i>{8 - Math.floor(index / 8)}</i>}</span>)}</div><div className="position-footer"><span>THE QUIET BEFORE THE COMBINATION</span><button type="button">Explore the position <ArrowUpRight size={14} /></button></div></section><section className="club-story"><span className="section-kicker">MORE THAN A BOARD</span><h1>Chess is better<br />when it <em>pulls you in.</em></h1><p>MoChess is a calm place to play serious games, notice beautiful ideas, and return tomorrow a little sharper.</p><div className="club-paths"><article><Swords size={18} /><div><strong>Find a game</strong><span>Play online at your pace.</span></div></article><article><Puzzle size={18} /><div><strong>See the tactic</strong><span>Puzzles worth thinking about.</span></div></article><article><BookOpen size={18} /><div><strong>Build your game</strong><span>Short lessons that stick.</span></div></article></div>{registeredUsers !== null && <p className="club-members"><b>{registeredUsers.toLocaleString()}</b> players have made this their club.</p>}</section><section className="club-account"><div className="account-mark"><Crown size={20} /></div><span className="section-kicker">{verificationEmail ? 'CONFIRM YOUR SEAT' : register ? 'A SEAT AT THE BOARD' : 'WELCOME BACK'}</span><h2>{verificationEmail ? 'One move left.' : register ? 'Start your first game.' : 'Ready when you are.'}</h2><p>{verificationEmail ? `Use the six-digit code sent to ${verificationEmail}.` : register ? 'Create a free account. Your games, puzzles, and progress will be waiting.' : 'Sign in to pick up your next game, lesson, or puzzle.'}</p>{message && <p role="status" className="access-message">{message}</p>}{error && <p role="alert" className="storage-notice">{error}</p>}<form onSubmit={submit}>{verificationEmail ? <><label>Verification code<input name="code" inputMode="numeric" autoComplete="one-time-code" pattern="[0-9]{6}" required maxLength={6} autoFocus /></label><button className="primary-action" disabled={busy}>{busy ? 'Verifying…' : 'Verify email'}</button><button className="secondary-action" type="button" disabled={busy} onClick={() => void api('/auth/resend-verification', { method: 'POST', body: JSON.stringify({ email: verificationEmail }) }).then(() => setMessage('A new code was sent.')).catch(value => setError(value instanceof Error ? value.message : 'Could not resend code.'))}>Send another code</button></> : <>{register && <label>Player name<input name="name" required minLength={2} maxLength={32} autoFocus /></label>}<label>Email<input name="email" type="email" required autoComplete="email" autoFocus={!register} /></label><label>Password<input name="password" type="password" required minLength={10} autoComplete={register ? 'new-password' : 'current-password'} /></label><button className="primary-action" disabled={busy}>{busy ? 'Please wait…' : register ? 'Create free account' : 'Sign in'}</button><p className="access-switch">{register ? 'Already play here?' : 'New to the club?'} <button type="button" disabled={busy} onClick={() => { setRegister(value => !value); setError('') }}>{register ? 'Sign in' : 'Create an account'}</button></p></>}</form><small>Free forever. No ads on the board.</small></section></div></main>
}
